Steps for execution in ubuntu:
1. Open the terminal in the directory of code file
2. Type: make compile // to run the command
3. Type: make execute // to execute the command
4. Enter the path of empty image
5. Enter the path of video